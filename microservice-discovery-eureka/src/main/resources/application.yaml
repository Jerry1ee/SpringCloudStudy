server:
  port: 8761

eureka:
  client:
#    默认交互地址
    service-url:
      defaultZone: http://localhost:8761/eureka/
#      不要注册自己，因为自己就是server
    register-with-eureka: false
#    不要同步注册服务列表，因为只有单节点
    fetch-registry: false
